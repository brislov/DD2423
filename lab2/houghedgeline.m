function [linepar acc] = houghedgeline(pic, scale, gradmagnthreshold, ...
                                       nrho, ntheta, nlines, verbose)
                                   
                                   